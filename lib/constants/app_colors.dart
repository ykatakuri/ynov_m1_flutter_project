import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.black;
  static const shadowColor = Colors.deepOrange;
  static const secondaryColor = Colors.white;
  static const progressBarColor = Colors.amber;
}
